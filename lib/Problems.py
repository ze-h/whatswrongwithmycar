class VehicleIssues:
    def __init__(self):
        self.dead_battery = float(0)
        self.faulty_starter = float(0)
        self.bad_ignition_switch = float(0)
        self.fuel_supply_issue = float(0)
        self.faulty_spark_plugs = float(0)
        self.broken_timing_belt = float(0)
        self.low_coolant = float(0)
        self.faulty_radiator = float(0)
        self.broken_water_pump = float(0)
        self.oil_leak = float(0)
        self.faulty_gasket = float(0)
        self.worn_piston_rings = float(0)
        self.sharp_object_in_tire = float(0)
        self.tire_wear = float(0)
        self.valve_stem_leak = float(0)
        self.misaligned_wheels = float(0)
        self.brake_issue = float(0)
        self.worn_out_tire = float(0)
        self.worn_brake_pads = float(0)
        self.loose_brake_parts = float(0)
        self.rusted_rotors = float(0)
        self.clogged_air_filter = float(0)
        self.faulty_oxygen_sensor = float(0)
        self.leaking_fuel_injector = float(0)
        self.low_transmission_fluid = float(0)
        self.worn_clutch = float(0)
        self.faulty_solenoids = float(0)
        self.out_of_balance_tires = float(0)
        self.warped_rotors = float(0)
        self.faulty_steering_mechanism = float(0)
        self.faulty_alternator = float(0)
        self.corroded_battery_terminals = float(0)
        self.blown_fuse = float(0)
        self.faulty_brake_booster = float(0)
        self.damaged_CV_joints = float(0)
        self.malfunctioning_mass_air_flow_sensor = float(0)
        self.clogged_fuel_filter = float(0)
        self.cracked_serpentine_belt = float(0)
        self.exhaust_leak = float(0)
        self.damaged_shock_absorbers = float(0)
        self.faulty_fuel_pump = float(0)
        self.sticking_brake_caliper = float(0)
        self.contaminated_brake_fluid = float(0)
        self.worn_drive_belt = float(0)
        self.malfunctioning_thermostat = float(0)
        self.clogged_radiator = float(0)
        self.malfunctioning_EGR_valve = float(0)
        self.faulty_transmission_control_module = float(0)
        self.damaged_wheel_bearing = float(0)
        self.leaking_power_steering_fluid = float(0)
        self.broken_ball_joints = float(0)
        self.damaged_tie_rods = float(0)
        self.bad_fuel_mixture = float(0)
        self.worn_distributor_cap = float(0)
        self.stuck_PCV_valve = float(0)
        self.malfunctioning_catalytic_converter = float(0)
        self.worn_spark_plug_wires = float(0)
        self.faulty_ignition_coil = float(0)
        self.clogged_cabin_air_filter = float(0)
        self.failed_blower_motor = float(0)
        self.leaky_radiator_hose = float(0)
        self.corroded_ground_wires = float(0)
        self.malfunctioning_windshield_wipers = float(0)
        self.faulty_turn_signal_relay = float(0)
        self.leaking_transmission_fluid = float(0)
        self.bad_wheel_alignment = float(0)
        self.damaged_exhaust_manifold = float(0)
        self.malfunctioning_speedometer = float(0)
        self.fading_car_paint = float(0)
        self.rust_on_body_panels = float(0)
        self.damaged_oxygen_sensors = float(0)
        self.faulty_camshaft_position_sensor = float(0)
        self.malfunctioning_ABS_system = float(0)
        self.failed_airbags = float(0)
        self.burned_out_headlights = float(0)
        self.cracked_windshield = float(0)
        self.unresponsive_electric_windows = float(0)
        self.faulty_central_locking_system = float(0)
        self.failing_air_conditioner_compressor = float(0)
        self.leaking_refrigerant = float(0)
        self.broken_fan_belt = float(0)
        self.malfunctioning_horn = float(0)
        self.faulty_voltage_regulator = float(0)
        self.worn_brake_shoes = float(0)
        self.faulty_throttle_position_sensor = float(0)
        self.malfunctioning_transmission_cooler = float(0)
        self.damaged_axle_shaft = float(0)
        self.stuck_fuel_injector = float(0)
        self.broken_motor_mounts = float(0)
        self.damaged_transmission_mounts = float(0)
        self.leaking_differential_fluid = float(0)
        self.worn_serpentine_belt_tensioner = float(0)
        self.cracked_windshield_wiper_fluid_reservoir = float(0)
        self.damaged_sway_bar_links = float(0)
        self.worn_control_arm_bushings = float(0)
        self.stuck_brake_pedal = float(0)
        self.burnt_clutch_disc = float(0)
        self.leaking_brake_master_cylinder = float(0)
        self.faulty_fuel_tank_sender = float(0)
        self.failed_emission_test = float(0)

